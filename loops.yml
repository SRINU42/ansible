- name: demo loop
  hosts: localhost
  connection: local
  tasks: 
    - name: fruits name
      ansible.builtin.debug:
        msg: fruit name - {{ item }}
      when: "{{ what }}"
      loop:
        - apple
        - orange
        - banana
        - grape
#      when: "{{ what }}"
